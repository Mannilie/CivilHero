using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ColorIdentify : MonoBehaviour
{
    //These are variables which are grabbed and used from the color apply script and also allows the player to destroy the whole object instead of each particular piece.
    public int WhichBin;
    public Material NatureColor;
    public Material RecycleColor;
    public Material GarbageColor;
    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
